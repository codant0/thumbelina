# 项目概要
学习项目，后续按需接入thumbelina

## 技术栈

* RAG框架：LlamaIndex （封装度高）
* 向量化数据存储：Chroma

## 上手

* 脚手架：可使用常用框架：LangChain、LlamaIndex等成熟框架。或不依赖框架进行原生开发
* 向量化存储
  * 大规模数据方案：Milvus、Pinecone
  * 轻量级或本地化方案：FAISS、Chroma

--- 

由于已学习过基本的Langchain内容，本次选择使用LlamaIndex框架进行。LlamaIndex优势：封装度更高，代码量更低

向量化存储选择Chroma方案

# RAG学习

原理：
1. **知识向量化**：本地知识向量化存储，用以后续相似度搜索，需要使用向量数据库
2. **语义召回**：用户检索时，根据相似度搜索，从向量数据库中召回相关片段
3. **上下文整合**：根据提示词整合召回片段，发送给LLM

## 数据准备

1. 数据加载：将各种非结构化文档，转换为程序可用的结构化数据。
2. 数据分块：将加载后的长篇文档，切分成文本块，是后续向量检索和模型处理的基本单位。

流程：
1. 分词：将文本块分为独立的Token
2. 向量化：为每个Token生成一个高维度向量表示
3. 池化：将分块中的所有Token**压缩成一个单一的向量**，这个向量代表整个文本块的语义。
   * case
     * 文本块：大鱼吃小鱼
     * Tokens：[大鱼]、[吃]、[小鱼]
     * 向量化（以三维举例，实际维度巨大）：[0.2, 0.8, 0.1]、[0.6, 0.3, 0.5]、[0.9, 0.1, 0.7]
     * 池化结果（使用最常见的池化方式-平均池化，每个维度相加求平均）：[0.566, 0.400, 0.433]

切块大小？
   * 切块不宜过大：首先不能超过所使用嵌入模型的上下文长度上限，否则超出部分会直接截断，导致信息丢失；检索后需要将取回的多个切块一并放入大模型的窗口中，若单个块过大，则可能只能容纳少数相关块（随着大模型上下文窗口增大，一般影响较小）。
   * 切块不宜过小：切块过小会导致上下文割裂，语义丢失

## 索引构建

### 向量嵌入
根据分块数据，使用Embedding模型，根据**语义**，在一个巨大维度里嵌入一个独一无二的坐标

### 向量数据库
后续考虑学习milvus数据库使用，目前先跳过，使用最简单的Chroma实现

## 检索优化

### 混合检索
结合稀疏向量和密集向量优势的搜索技术


## 效果评估

1. 检索相关性（是否包含答案）
2. 生成质量：回答的意思是否正确、专业术语是否使用得当
3. 多跳推理？